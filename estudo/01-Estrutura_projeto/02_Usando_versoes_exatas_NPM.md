# Vamos configurar o NPM (GERENCIADOR DE PACOTES NODE)

Vamos passar a seguinte configuração:
`save-exact=true`
Oq essa configuração faz.
A partir de agora quando executarmos um npm i, seja uma dependencia de desenvolvimento ou de produção, a versão da dependência que vai ficar dentro do package.json vai ficar fixa.
